import solve from './index';

describe('Day1', () => {
    test('Easy input', () => {
        const input = ``;
        const result = solve(input);
        expect(result.part1).toBe('Résultat Partie 1');
        expect(result.part2).toBe('Résultat Partie 2');
    });

    test('Intermediate input', () => {
        const input = ``;
        const result = solve(input);
        expect(result.part1).toBe('Résultat Partie 1');
        expect(result.part2).toBe('Résultat Partie 2');
    });

    test('Difficult input', () => {
        const input = ``;
        const result = solve(input);
        expect(result.part1).toBe('Résultat Partie 1');
        expect(result.part2).toBe('Résultat Partie 2');
    });
});
